#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for (int i=0;i<n;i++)
#define rrep(i,n) for (int i=1;i<=n;i++)

using ll=long long;
int main()
{
    int a,b;
    cin >> a >> b;
    int sum = a + b;
    if(sum==0)cout << 1;
    else cout << 0;

    return 0;
}